class Solution:
    def minimumSum(self, num: int) -> int:
        num=sorted(list(str(num)))
             
        new1=int(num[0])*10+int(num[3])
        new2=int(num[1])*10+int(num[2])
        return new1+new2
